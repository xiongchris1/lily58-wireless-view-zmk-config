# Enable Deep Sleep
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000

# Increase wireless range
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Eager debouncing
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=1
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=10

# 1. Enable ZMK Display
CONFIG_ZMK_DISPLAY=y
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM=y

# 2. Unlock the Gengar Module (THE MASTER SWITCH)
CONFIG_NICE_OLED=y

# 3. Enable Gengar Animation
CONFIG_NICE_OLED_ANIMATION_POKEMON=y

# 4. Enable e-Paper Mode
CONFIG_NICE_EPAPER_ON=y

# 5. Add Widgets
CONFIG_NICE_OLED_WIDGET_BATTERY=y
CONFIG_NICE_OLED_WIDGET_WPM=y
